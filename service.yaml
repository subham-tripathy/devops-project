apiVersion: v1
kind: Service
metadata:
  name: failure-detect-service
  labels:
    app: failure-detect
spec:
  type: NodePort
  selector:
    app: failure-detect
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30000
